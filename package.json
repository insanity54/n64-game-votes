{
  "name": "insanity54-game-votes",
  "version": "0.1.0",
  "description": "11ty site for generating game vote tables",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve",
    "clean": "rm -rf _site .cache"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-fetch": "^5.1.3"
  }
}